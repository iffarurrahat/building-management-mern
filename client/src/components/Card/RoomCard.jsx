const RoomCard = ({ room }) => {
  // const { image, floor-no, block-name, apartment-no, rent } = room || {};
  // console.log(room);

  return (
    <div>
      <p>Lorem ipsum dolor sit amet.</p>
    </div>
  );
};

export default RoomCard;
